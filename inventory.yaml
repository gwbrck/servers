all:
  vars:
    ansible_user: gwbrck
  children:
    managed_servers:
      hosts:
        pivpn:
        homeeins-1:
        hermes:
    docker_hosts:
      hosts:
        pivpn:
        homeeins-1:
    application_hosts:
      hosts:
        homeeins-1:
    sync_hosts:
      hosts:
        pivpn:
