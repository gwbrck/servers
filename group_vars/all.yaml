sops_file: "{{ lookup('env', 'HOME') }}/Documents/Dots/server_infra.yaml"
sops: "{{ lookup('community.sops.sops', sops_file) | from_yaml }}"
reverse_proxy_network_name: "{{ sops.proxy.network_name | default('traefik-net', true) }}"
reverse_proxy_cert_resolver: "{{ sops.proxy.cert_resolver | default('dnshetzner', true) }}"
